
  # widget

  This is a code bundle for widget. The original project is available at https://www.figma.com/design/lL6TxaJzk5iY7VGuW03lHo/widget.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  